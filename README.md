# Landing Page

Open `index.html` in a browser to view the local offer page.

Open `questionnaire.html` to view the async intake form.

Before using publicly:

1. Review `privacy.html` and replace it with a solicitor-reviewed privacy policy before scaling.
2. Host on a simple static host such as Netlify, Carrd, Framer, Webflow, or GitHub Pages.
